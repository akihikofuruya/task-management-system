# 開発ログ

## プロジェクト名

個人向けタスク管理システム

## 2026-07-09

### 実施内容

#### 要件定義書の作成

システム開発の学習を目的として、個人向けタスク管理システムの要件定義書の作成を開始した。

以下の章を作成した。

- 第1章 プロジェクト概要
- 第2章 システム概要
- 第3章 業務概要
- 第4章 機能要件
- 第5章 非機能要件
- 第6章 画面一覧
- 第7章 データ要件
- 第8章 制約事項
- 第9章 開発スケジュール

#### 機能要件の整理

以下の機能を定義した。

- ログイン
- ログアウト
- タスク一覧表示
- タスク登録
- タスク詳細表示
- タスク編集
- タスク削除
- タスク検索
- ステータス変更
- PDF出力

#### 非機能要件の整理

以下の内容を定義した。

- 性能要件
- セキュリティ要件
- 可用性要件
- バックアップ・リストア
- 個人情報の取り扱い
- データ暗号化
- データ保持期間
- データ削除ルール

#### 画面一覧の作成

以下の画面を定義した。

- SCR-001 ログイン画面
- SCR-002 タスク一覧画面
- SCR-003 タスク登録画面
- SCR-004 タスク詳細画面
- SCR-005 タスク編集画面

### 学び・気付き

要件定義では、「画面」と「機能」は必ずしも一致しないことを学んだ。

例えば、以下は独立した画面ではなく、タスク一覧画面の機能として実装する設計とした。

- ログアウト
- PDF出力
- タスク検索

## 2026-07-10

### 実施内容

#### 要件定義書の見直し

要件定義書全体を確認し、各章の内容を整理した。

#### 画面遷移図の修正

要件定義書の画面遷移図について、画面と機能を整理し、シンプルな画面遷移図へ修正した。

#### データ要件の整理

以下の内容を追加した。

- データ容量の見積もり
- 保持期間
- データ削除ルール
- バックアップ・リストア方針
- 個人情報の取り扱い
- データ暗号化

#### 開発スケジュールの整理

プロジェクト計画書を作成しない前提で、要件定義からリリースまでのスケジュールを整理した。

### 学び・気付き

要件定義書は一度作成したら終わりではなく、レビューや設計を通じて改善していく文書であることを理解した。

また、要件定義では「何を作るか」を定義し、設計工程では「どのように作るか」を定義するという役割の違いを学んだ。

## 2026-07-13

### 実施内容

#### リポジトリの準備

- GitHub リポジトリ `task-management-system` をローカルの `C:\workspace` にクローン
- タスク管理システムの最小構成を `README.md` に整理
- 要件定義書とガントチャートをリポジトリに追加
- 初回コミットを作成し、`main` ブランチへプッシュ
- 開発ログ用の `docs/development-log.md` を作成

#### 基本設計書の作成

要件定義書をもとに、個人向けタスク管理システムの基本設計書を作成した。

基本設計では、主に以下の内容を整理した。

- システム構成
- 画面設計
- 画面遷移
- 機能設計
- データ設計
- テーブル構成
- 入力項目
- バリデーション方針
- セキュリティ方針
- エラー処理方針

#### 基本設計書の確認

要件定義書で整理した内容と基本設計書の内容を照らし合わせ、必要な機能や画面が設計に反映されていることを確認した。

### 現時点の成果物

- `README.md`
- `1.2_要件定義書タテ.docx`
- `ガントチャートテンプレート.xlsx`
- `docs/development-log.md`
- 基本設計書

### 学び・気付き

基本設計では、要件定義で決めた「何を作るか」をもとに、画面、機能、データの単位でシステム全体の形を整理することが重要だと分かった。

また、基本設計書を作成することで、実装前に必要な画面、テーブル、入力項目、処理の流れを確認できるため、後工程の手戻りを減らせると感じた。

### 次にやること

- 詳細設計書を作成する
- 各機能の処理内容をより具体的に整理する
- 各画面の入力チェック、ボタン操作、エラー表示を整理する
- テーブル定義を詳細化する
- Spring Boot / PostgreSQL / Docker の開発環境構築に向けた準備を行う

## 2026-07-14

### 実施内容

#### 詳細設計書の作成

基本設計書をもとに、詳細設計書の作成を開始した。

以下の章・項目について整理を行った。

- 開発環境
- 共通設計
- パッケージ構成
- クラス一覧
- Controller設計
- Service設計
- Repository設計
- DTO設計
- Entity設計
- シーケンス図

#### 開発環境の整理

Java / Spring Boot / PostgreSQL / Docker を前提とした開発環境の構成を整理した。

#### 共通設計の整理

アプリケーション全体で共通して利用する設計方針を整理した。

主に以下の観点を確認した。

- レイヤー構成
- 命名規則
- バリデーション方針
- 例外処理方針
- 認証・認可の考え方

#### パッケージ構成・クラス一覧の整理

Spring Boot の構成を前提に、Controller、Service、Repository、DTO、Entity の責務を分けて整理した。

また、各クラスがどの役割を持つかを一覧化し、実装時に迷わないようにした。

#### 各レイヤーの設計

以下のレイヤーについて、役割と処理内容を整理した。

- Controller: 画面やリクエストの受付、入力値の受け渡し
- Service: 業務ロジック、トランザクション制御
- Repository: DBアクセス
- DTO: 画面・フォーム・レイヤー間のデータ受け渡し
- Entity: DBテーブルに対応するデータ構造

#### シーケンス図の作成

主要な処理の流れをシーケンス図として整理した。

画面操作から Controller、Service、Repository、DB までの流れを確認し、実装時の処理順序を明確にした。

### 学び・気付き

詳細設計では、基本設計で整理した内容を、実装できる粒度まで具体化することが重要だと分かった。

特に、Controller、Service、Repository、DTO、Entity の責務を分けて整理することで、どこに何を書くべきかが明確になった。

また、シーケンス図を作成することで、画面操作からDB処理までの流れを事前に確認でき、実装時の認識ズレを減らせると感じた。

### 次にやること

- 詳細設計書の残りの章を整理する
- 画面ごとの入力チェック仕様を整理する
- エラー処理とメッセージ仕様を整理する
- テスト仕様書の作成に進む
- 実装に向けて Spring Boot / PostgreSQL / Docker の構成を準備する

## 2026-07-15

### 実施内容

#### Spring Boot プロジェクトの実装準備

詳細設計書で整理した構成をもとに、Spring Boot アプリケーションとして実装を進めるための土台を整備した。

主に以下の内容を追加・整理した。

- Maven 依存関係の整理
- PostgreSQL 接続設定
- Docker Compose による PostgreSQL 起動設定
- JPA Entity の作成
- Repository インターフェースの作成

#### User Entity の作成

ユーザー情報を管理する `User` Entity を作成した。

以下の項目を保持する構成とした。

- ユーザーID
- 名前
- メールアドレス
- パスワード
- 作成日時
- 更新日時

メールアドレスはユーザーを一意に識別する項目として扱うため、DB上でユニーク制約を設定した。

また、作成日時と更新日時は `@PrePersist`、`@PreUpdate` を利用して自動的に設定・更新されるようにした。

#### Task Entity の作成

タスク情報を管理する `Task` Entity を作成した。

以下の項目を保持する構成とした。

- タスクID
- ユーザー
- タイトル
- 内容
- 期限日
- 優先度
- ステータス
- 作成日時
- 更新日時

タスクはユーザーに紐づく情報であるため、`User` と `Task` を 1対多 の関連として定義した。

#### UserRepository の作成

`UserRepository.java` を作成し、`JpaRepository<User, Long>` を継承してユーザー情報へアクセスできるようにした。

追加した検索メソッドは以下の通り。

- `findByEmail(String email)`
- `existsByEmail(String email)`

`findByEmail` はログイン処理などでメールアドレスからユーザーを取得するために使用する想定とした。

`existsByEmail` はユーザー登録時に、同じメールアドレスが既に登録されていないか確認するために使用する想定とした。

戻り値に `Optional<User>` を利用することで、ユーザーが存在しない場合の処理を呼び出し側で明確に扱えるようにした。

#### TaskRepository の作成

`TaskRepository` を作成し、ユーザー単位でタスクを取得できるようにした。

以下の条件で検索できるメソッドを用意した。

- ユーザーID
- ステータス
- 優先度
- 期限日
- タイトルのキーワード

タスク一覧画面や検索機能で必要になる条件を、Repository のメソッドとして先に整理した。

### 学び・気付き

Spring Data JPA では、`JpaRepository` を継承するだけで基本的なCRUD操作を利用できるため、Repository では業務上必要な検索条件を中心に定義すればよいことが分かった。

また、`findByEmail` や `existsByEmail` のようにメソッド名からクエリが生成されるため、単純な検索であれば SQL を直接書かずに実装できる点が便利だと感じた。

Entity では、テーブルの項目だけでなく、制約、関連、初期値、日時更新のタイミングも意識して定義する必要があると分かった。

### 次にやること

- Service クラスを作成する
- Controller クラスを作成する
- ユーザー登録・ログイン処理を実装する
- タスク登録・一覧表示・検索処理を実装する
- Repository のテストを作成する

## 2026-07-17

### これからやるべきこと

#### Service 層の作成

Repository まで作成できているため、次は業務ロジックを担当する Service 層を作成する。

まずは以下の Service を作成する。

- `UserService`
- `TaskService`

`UserService` では、ユーザー登録、メールアドレス重複チェック、ログイン時のユーザー取得を扱う。

`TaskService` では、タスク登録、一覧取得、詳細取得、更新、削除、検索を扱う。

#### DTO・フォームクラスの作成

Controller と Service、画面入力値の受け渡しを分かりやすくするため、DTO・フォームクラスを作成する。

まずは以下の入力用クラスを整理する。

- ユーザー登録用フォーム
- ログイン用フォーム
- タスク登録用フォーム
- タスク更新用フォーム
- タスク検索用フォーム

Entity をそのまま画面入力に使わず、入力値とDB構造の責務を分ける。

#### Controller 層の作成

画面からのリクエストを受け取る Controller を作成する。

優先して作成する Controller は以下の通り。

- ユーザー登録 Controller
- ログイン Controller
- タスク一覧 Controller
- タスク登録 Controller
- タスク編集 Controller
- タスク削除 Controller

Controller では入力値を受け取り、Service を呼び出し、処理結果に応じて画面遷移を制御する。

#### 画面テンプレートの作成

Controller の作成に合わせて、画面テンプレートを作成する。

まずは以下の画面を作成する。

- ユーザー登録画面
- ログイン画面
- タスク一覧画面
- タスク登録画面
- タスク編集画面
- タスク詳細画面

入力チェックエラーや登録完了メッセージも画面に表示できるようにする。

#### バリデーションとエラー処理の実装

フォーム入力に対して、必須チェック、文字数チェック、日付チェックを実装する。

特に以下の項目を優先して確認する。

- メールアドレスの形式
- メールアドレスの重複
- パスワードの未入力
- タスクタイトルの未入力
- 期限日の未入力
- 優先度・ステータスの値

エラーが発生した場合は、画面に分かりやすいメッセージを表示する。

#### Repository・Service のテスト作成

`UserRepository` の `findByEmail`、`existsByEmail` が想定通り動作することを確認する。

また、`TaskRepository` のユーザーID、ステータス、優先度、期限日、タイトル検索についてもテストを作成する。

Service 層では、正常系だけでなく、メールアドレス重複や存在しないタスクを操作した場合の異常系も確認する。

#### 実装の優先順

次の順番で進める。

1. DTO・フォームクラスを作成する
2. `UserService` を作成する
3. `TaskService` を作成する
4. Controller を作成する
5. 画面テンプレートを作成する
6. バリデーションとエラー処理を追加する
7. Repository・Service のテストを作成する
8. 画面から一連の操作を確認する
